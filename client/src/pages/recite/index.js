import React, { PureComponent } from 'react'
import { connect } from 'react-redux'

class Recite extends PureComponent {
  render() {
    return <div>背单词</div>
  }
}

export default connect(null, null)(Recite)
